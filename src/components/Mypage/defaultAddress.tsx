import React from "react";
import { NODATA } from "@/components/Mypage/cart";

export default function DefaultAddress() {
  return (
    <div>
      <h2>기본 배송지</h2>
      <NODATA>첫 주문을 진행하고 기본 배송지를 등록해보세요.</NODATA>
    </div>
  );
}
